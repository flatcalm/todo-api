
server:
  port: 8181

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=Asia/Seoul
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    # ??? ?? ? ?? DB? ?? ???? ??
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    database: mysql # DB ?? ??

logging.level:
  org.hibernate.SQL: debug

# swagger setting
spring-doc:
  swagger-ui:
    path: /myapi.html

jwt:
  secret: RHnMgCgigH9zS8wvNuSoP/pUfGSqSbarlk+5ku+xPWvESIaisvOlIubaUCbDvUVko66n7NSJprsMdpj28YGEKQ==
