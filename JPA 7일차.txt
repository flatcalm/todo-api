사용자 정의 예외
자바에서 지원하지 않는 예외를 생성할 때

전역 CORS 설정
컨트롤러마다 @CrossOrigin 어노테이션을 쓸 필요 없이 해당 프로젝트를 모두 허용해주고 싶을 때 설정하는 방법
Config 패키지에 CorsConfig 클래스 생성
maxAge(3600)
동일한 요청이 반복으로 들어올 때 서버 과부하를 줄여주기 위한 캐싱 시간을 설정해주는 메서드.
캐싱 시간 동안 동일한 요청이 들어오면 요청을 처리하는 대신 캐시 메모리에 저장된 데이터를 줌.

JSON Web Token(JWT)
쿠키와 세션의 단점을 보완하기 위해서 사용

auth 패키지 생성 TokenProvider 클래스 생성
토큰을 발급하고, 서명 위조를 검사할 객체

JWT에서는 서명에 사용할 값을 512비트 이상의 랜덤 문자열을 사용할 것을 권장

나중에는 yml 파일도 gitignore에 등록해야함. (properties처럼 중요 값도 저장하기 때문에)

/**
 *
 * @param
 * @return
 */ 주석
메서드를 설명할 때 사용
/** 입력 후 엔터 치면 자동 완성
/**
 * 메서드에 대한 간단한 설명
 * @param 입력 받는 매개 변수에 대한 설명
 * @return 리턴되는 값에 대한 설명
 */
형식으로 작성.


유저 권한 관리를 위해 User 클래스로 이동하여 role 필드 추가.
Alt + Enter 이용하여 Entity 패키지에 열거형(Enum) 타입으로 Role 생성.
숫자로 자동 변환하여 저장하는 걸 막기 위해 @Enumerated(EnumType.STRING) 어노테이션 추가.
(변환 없이 열거형에 지정한 문자열 타입 그대로 DB에 추가)
DB 컬럼의 기본값 설정을 위해 @ColumnDefault("'COMMON'") 어노테이션 추가
열거형(ENUM) 타입은 ''(홑따옴표)로 한번 더 감싸서 작성.
